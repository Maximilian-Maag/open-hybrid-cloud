apiVersion: v1
kind: Service
metadata:
  name: {{ include "infra-webshop.fullname" . }}
  labels:
    {{- include "infra-webshop.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infra-webshop.selectorLabels" . | nindent 4 }}

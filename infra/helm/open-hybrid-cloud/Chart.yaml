apiVersion: v2
name: open-hybrid-cloud
description: Open Hybrid Cloud Infrastructure Self-Service Webshop
type: application
version: 0.1.0
# The image tag deployed when `frontend.image.tag` / `backend.image.tag` are left
# empty — see _helpers.tpl. It was "latest", which meant a chart nobody edited
# deployed a different image on every pull (#180). Kept in step with the chart
# version, and CI overrides it per release.
appVersion: "0.1.0"
keywords:
  - open-hybrid-cloud
  - infrastructure
  - self-service
maintainers:
  - name: Open Hybrid Cloud Digital Unit

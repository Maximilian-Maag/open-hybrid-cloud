Open Hybrid Cloud deployed successfully.

{{- if .Values.migrate.enabled }}
Database migrations were run as a pre-install/pre-upgrade hook.
{{- end }}

{{- if .Values.ingress.enabled }}
The application is available at:
{{- range .Values.ingress.hosts }}
  https://{{ .host }}
{{- end }}
{{- else }}
No Ingress is configured. Access the services via port-forward:

  # Frontend
  kubectl port-forward svc/{{ include "open-hybrid-cloud.frontend.fullname" . }} 3000:{{ .Values.frontend.service.port }} -n {{ .Release.Namespace }}

  # Backend API
  kubectl port-forward svc/{{ include "open-hybrid-cloud.backend.fullname" . }} 3001:{{ .Values.backend.service.port }} -n {{ .Release.Namespace }}

Then open: http://localhost:3000
{{- end }}

Initial admin credentials:
  Email:    {{ .Values.backend.env.adminEmail }}
  Password: (value of backend.secrets.adminPassword — change immediately after first login)

To check rollout status:
  kubectl rollout status deployment/{{ include "open-hybrid-cloud.frontend.fullname" . }} -n {{ .Release.Namespace }}
  kubectl rollout status deployment/{{ include "open-hybrid-cloud.backend.fullname" . }} -n {{ .Release.Namespace }}
